# Dependencies and Next.js build output
node_modules/
.next/
*.tsbuildinfo

# Local secrets. Keep the safe template under version control.
.env*
!.env.example

# Timestamped database exports are local recovery artifacts, never source data.
backups/

# Local extraction evaluation history includes model outputs and is intentionally
# reproducible locally, but never committed as product data.
evals/
