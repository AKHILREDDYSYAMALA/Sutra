# Required for live PDF analysis. The static sandbox works without this value.
OPENAI_API_KEY=

# Optional: replace the safe default in lib/extraction-prompt.ts with your final extraction prompt.
SUTRA_EXTRACTION_SYSTEM_PROMPT=
